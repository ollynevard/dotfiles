# Apps
cmd - 1 [
  "wezterm" : skhd -k "cmd -h"
  * : open -a wezterm
]

cmd - 2 [
  "firefox" : skhd -k "cmd -h"
  * : open -a firefox
]

cmd - 3 [
  "slack" : skhd -k "cmd -h"
  * : open -a "slack"
]

cmd - 4 [
  "microsoft teams" : skhd -k "cmd -h"
  * : open -a "microsoft teams"
]

cmd - 5 [
  "whatsapp" : skhd -k "cmd -h"
  * : open -a "whatsapp"
]

