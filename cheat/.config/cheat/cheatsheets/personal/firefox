# Firefox

https://support.mozilla.org/en-US/kb/keyboard-shortcuts-perform-firefox-tasks-quickly

## Modifier keys

⌘   Command (or Cmd)
⌥   Option (or Alt)
^   Control (or Ctrl)
⇧   Shift
fn  Function

## Navigation

⌘ + [      Back
⌘ + ]      Forward
⌘ + R      Reload
⌘ + ⇧ + R  Reload (override cache)
esc        Stop

## Windows and tabs

⌘ + Q              Quit

⌘ + N              New window
⌘ + ⇧ + P          New private window
⌘ + ⇧ + W          Close window
⌘ + ⇧ + N          Undo close window

⌘ + T              New tab
⌘ + W              Close tab
⌘ + ⇧ + T          Undo close tab

^ + page up        Go one tab to the left
^ + page down      Go one tab to the right
^ + ⇧ + page up    Move tab left
^ + ⇧ + page down  Move tab right

## Miscellaneous

⌘ + L  Focus address bar

